apiVersion: v1
kind: Service
metadata:
  name: {{ include "djl-serving.fullname" . }}
  labels:
    {{- include "djl-serving.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "djl-serving.selectorLabels" . | nindent 4 }}
